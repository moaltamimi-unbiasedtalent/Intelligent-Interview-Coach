"""FastAPI backend for Intelligent Interview Coach (Sprint 4 Phase 2).

A thin HTTP layer over ``src/application``. Import ``src.api.main:app`` to serve.
Importing this package performs no provider call, retrieval, evaluation or DB
migration — expensive resources are built lazily per the app lifespan.
"""
